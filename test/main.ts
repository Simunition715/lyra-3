import * as Lib from "../lib";

async function start() {
  const container = document.getElementById("main");
  if (!container) return;

  // TODO: Write your custom test front-end program here
  console.warn(Lib);
}

start();
